[build]
  base = "site/"
  command = "hugo -v --gc --minify"
  publish = "site/public"

[context.production.environment]
  HUGO_VERSION = "0.73.0"

[context.deploy-preview.environment]
  HUGO_VERSION = "0.73.0"
